# MASS - Metrics Analytics Super System

**MASS** (Metrics Analytics Super System) - универсальная система аналитики метрик с поддержкой различных источников данных.

## 🎯 Возможности

- 📊 **Анализ метрик** - автоматическое обнаружение деградаций, улучшений и аномалий
- 🔍 **Отслеживание контекстов** - обнаружение появления и исчезновения новых сущностей (например, ошибок)
- 📈 **Визуализация** - интерактивные графики с использованием Plotly
- 🎨 **Веб-интерфейс** - конструктор конфигураций и запуск аналитики
- 🔌 **Адаптеры данных** - поддержка различных источников данных (YDB, и другие в будущем)

## 📦 Установка

### Автоматическая установка (рекомендуется):

```bash
# Клонировать репозиторий
git clone https://github.com/ydb-platform/mass.git
cd mass

# Запустить скрипт установки
chmod +x setup.sh
./setup.sh
```

Скрипт автоматически:
- Создаст виртуальное окружение `venv`
- Установит все зависимости
- Установит пакет в режиме разработки
- Создаст конфиг YDB из примера (если нужно)

### Ручная установка:

```bash
# Клонировать репозиторий
git clone https://github.com/ydb-platform/mass.git
cd mass

# Создать виртуальное окружение
python3 -m venv venv

# Активировать виртуальное окружение
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate     # Windows

# Установить зависимости
pip install --upgrade pip
pip install -r requirements.txt

# Установить пакет в режиме разработки
pip install -e .
```

**Примечание:** Рекомендуется использовать Python 3.10, 3.11 или 3.12 (не 3.14+ из-за зависимостей prophet/numba).

## 🚀 Быстрый старт

### 1. Настройка переменных окружения

Для работы с YDB необходимо настроить переменные окружения:

```bash
export CI_YDB_SERVICE_ACCOUNT_KEY_FILE_CREDENTIALS=/path/to/credentials.json
```

Или создать файл `.env` в корне проекта:

```bash
CI_YDB_SERVICE_ACCOUNT_KEY_FILE_CREDENTIALS=/path/to/credentials.json
```

### 2. Настройка конфигурации YDB

Скопируйте пример конфига и настройте его:

```bash
cp config/ydb_qa_config.json.example config/ydb_qa_config.json
# Отредактируйте config/ydb_qa_config.json с вашими настройками
```

Конфиг содержит настройки подключения к YDB (endpoint, path, таблицы). По умолчанию `ydb_wrapper` ищет конфиг в `config/ydb_qa_config.json` относительно корня проекта.

### 3. Создание конфигурации

Создайте YAML конфигурационный файл (см. примеры в `configs/`):

```yaml
job:
  name: "my_analysis"
  description: "Описание анализа"

data_source:
  ydb:
    query: |
      SELECT
        timestamp as ts,
        metric_name,
        metric_value
      FROM `my_table`
      WHERE timestamp >= CurrentUtcTimestamp() - 30 * Interval("P1D")

context_fields: ["metric_name"]
metric_fields: ["metric_name", "metric_value"]
timestamp_field: "ts"

analytics:
  baseline_method: "rolling_mean"
  window_size: 7
  sensitivity: 2.0
  min_absolute_change: 5.0
  min_relative_change: 0.1
  hysteresis_points: 3
  adaptive_threshold: true
  min_data_points: 3

events:
  detect:
    - degradation_start
    - improvement_start
  output_table: "analytics/events"
  min_event_duration_minutes: 60

thresholds:
  output_table: "analytics/thresholds"
  keep_history: true

output:
  write_to_ydb: true
  log_to_console: true
  dry_run: false
```

### 4. Запуск аналитики

#### Через командную строку:

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate  # если еще не активировано

# Установленный пакет
mass configs/my_config.yaml

# Или напрямую через Python
python -m mass.core.analytics_job configs/my_config.yaml

# Dry run (без сохранения в БД)
python -m mass.core.analytics_job configs/my_config.yaml --dry-run
```

#### Через веб-интерфейс:

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate  # если еще не активировано

cd mass/ui
python app.py
```

Откройте в браузере: http://127.0.0.1:5000

## 📚 Архитектура

### Адаптеры данных

MASS использует паттерн адаптеров для поддержки различных источников данных:

- **YDBAdapter** - адаптер для YDB (Yandex Database)
- В будущем: PostgreSQL, MySQL, CSV и другие

### Основные модули

- **DataAccess** - загрузка данных через адаптеры
- **Preprocessing** - очистка и агрегация данных
- **BaselineCalculator** - расчет baseline и порогов
- **EventDetector** - обнаружение событий (деградации, улучшения)
- **ContextTracker** - отслеживание появления/исчезновения контекстов
- **Persistence** - сохранение результатов через адаптеры
- **Visualization** - генерация графиков
- **SummaryReport** - генерация HTML отчетов

## 🔧 Конфигурация

Подробное описание всех параметров конфигурации см. в `configs/example.yaml` и `configs/read_me.md`.

### Основные секции:

- **job** - название и описание задачи
- **data_source** - источник данных (ydb, query)
- **context_fields** - поля для группировки (например, error, operation_type)
- **metric_fields** - поля метрик (metric_name, metric_value)
- **analytics** - параметры анализа (baseline_method, sensitivity и т.д.)
- **events** - типы событий для обнаружения
- **context_tracking** - настройки отслеживания контекстов
- **metric_direction** - направление метрик (positive/negative)

## 📊 Визуализация

Система генерирует:

- **Интерактивные графики** (Plotly HTML) для каждого контекста с событиями
- **Сводные отчеты** (HTML) со списком всех событий, сгруппированных по контекстам

## 🧪 Тестирование

```bash
# Запустить тесты
python -m pytest tests/

# Или конкретный тест
python tests/test_event_detection.py
```

## 📝 Лицензия

Apache 2.0

## 🤝 Вклад

Приветствуются pull requests и issues!

## 📖 Документация

Дополнительная документация доступна в:
- `configs/read_me.md` - описание конфигураций
- `configs/example.yaml` - примеры конфигураций

