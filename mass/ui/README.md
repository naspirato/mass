# MASS - Metric Analytic Super System

Веб-интерфейс для работы с системой аналитики метрик.

## Возможности

- 📝 Визуальный конструктор конфигурационных файлов
- ✏️ Редактирование существующих конфигов
- 💾 Сохранение конфигов
- ▶️ Запуск аналитики прямо из интерфейса
- 📊 Просмотр отчетов

## Установка

```bash
cd .github/scripts/analytics/ydb_metrics_analytics_system/mas_ui
pip install -r requirements.txt
```

## Настройка переменных окружения

Для работы с YDB необходимо настроить переменные окружения. Есть несколько способов:

### Способ 1: Файл .env (рекомендуется)

Создайте файл `.env` в одной из следующих директорий (проверяется в указанном порядке):
- `.github/scripts/analytics/ydb_metrics_analytics_system/mas_ui/.env`
- `.github/scripts/analytics/ydb_metrics_analytics_system/.env`
- `.github/scripts/analytics/.env`

Содержимое файла:
```bash
CI_YDB_SERVICE_ACCOUNT_KEY_FILE_CREDENTIALS=/path/to/your/credentials.json
```

Или если у вас переменная уже экспортирована в `.bashrc`, вы можете создать `.env` файл так:
```bash
# В терминале выполните:
echo "CI_YDB_SERVICE_ACCOUNT_KEY_FILE_CREDENTIALS=$CI_YDB_SERVICE_ACCOUNT_KEY_FILE_CREDENTIALS" > .github/scripts/analytics/ydb_metrics_analytics_system/mas_ui/.env
```

### Способ 2: Экспорт перед запуском

```bash
export CI_YDB_SERVICE_ACCOUNT_KEY_FILE_CREDENTIALS=/path/to/your/credentials.json
python app.py
```

### Способ 3: Указать путь к файлу с credentials

Если credentials хранятся в файле, можно указать путь к файлу:
```bash
export CI_YDB_SERVICE_ACCOUNT_KEY_FILE_CREDENTIALS_FILE=/path/to/credentials.json
python app.py
```

## Запуск

```bash
python app.py
```

Или с параметрами:

```bash
python app.py --host 0.0.0.0 --port 5000
```

После запуска откройте в браузере: http://127.0.0.1:5000

## Использование

1. **Создание конфига**: Заполните форму и нажмите "Сгенерировать конфиг"
2. **Редактирование**: Выберите режим "Редактирование существующего" и загрузите YAML файл
3. **Сохранение**: Укажите имя файла и нажмите "Сохранить конфиг"
4. **Запуск аналитики**: Нажмите "Запустить аналитику" (конфиг будет сохранен автоматически, если указано имя)
5. **Просмотр отчетов**: Нажмите "Обновить список отчетов" и откройте нужный отчет

## API Endpoints

- `GET /` - Главная страница
- `GET /api/configs` - Список конфигов
- `POST /api/save-config` - Сохранить конфиг
- `POST /api/run` - Запустить аналитику
- `GET /api/reports` - Список отчетов
- `GET /api/report/<filename>` - Получить отчет

